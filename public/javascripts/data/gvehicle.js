// Generated by CoffeeScript 1.6.3
(function() {
  var GVehicle;

  GVehicle = (function() {
    GVehicle.prototype.GV_CRIT_1 = 'Crew Stunned (no actions next 2 turns';

    GVehicle.prototype.GV_CRIT_2 = 'Main Weapon Jams (no firing from largest system for 1 turn';

    GVehicle.prototype.GV_CRIT_3 = 'Engine Hit (no movement/rest of game';

    GVehicle.prototype.GV_CRIT_4 = 'Crew Killed (vehicle out of game)';

    GVehicle.prototype.GV_CRIT_5 = 'Fuel Tank Hit (vehicle explodes)';

    GVehicle.prototype.GV_CRIT_6 = 'Ammo/Power Plant Hit (vehicle explodes)';

    GVehicle.prototype.getWeightClass = function() {
      if (this.GV_TONNAGE <= 35) {
        'LIGHT';
      }
      if (this.GV_TONNAGE >= 40 && this.TONNAGE <= 55) {
        'MEDIUM';
      }
      if (this.GV_TONNAGE >= 60 && this.TONNAGE <= 75) {
        'HEAVY';
      }
      if (this.GV_TONNAGE >= 80) {
        return 'ASSAULT';
      }
    };

    function GVehicle() {
      var part, part_info;
      for (part in PARTS) {
        part_info = PARTS[part];
        this['armor_' + part] = this['ARMOR_' + part];
      }
      for (part in PARTS) {
        part_info = PARTS[part];
        this['structure_' + part] = this['STRUCTURE_' + part];
      }
    }

    return GVehicle;

  })();

}).call(this);
